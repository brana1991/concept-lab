{
  "name": "pdf-converter",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "sharp": "^0.30.0",
    "cli-progress": "^3.9.1",
    "pg": "^8.10.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "scripts": {
    "convert": "node convert.mjs",
    "api": "node api-server.mjs",
    "api:dev": "node --watch api-server.mjs",
    "api:nodemon": "nodemon api-server.mjs",
    "dev": "docker compose -f docker-compose.dev.yml up -d",
    "dev:api": "docker compose -f docker-compose.dev.yml up api-server",
    "dev:convert": "docker compose -f docker-compose.dev.yml up pdf-converter",
    "build": "./rebuild.sh",
    "watch": "./watch.sh"
  }
}
