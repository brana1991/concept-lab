{
  "name": "concept-lab",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "clean": "lerna clean && yarn cache clean"
  },
  "devDependencies": {
    "lerna": "^7.1.4"
  }
}
